# Bundle-size budget history

The four byte budgets in `scripts/ci/check-bundle-size.sh` get bumped
when real features need the room. This is the running record of why
each bump happened, so a future maintainer doesn't have to
`git blame` a single overgrown comment.

Append a row to the table when you bump a budget. Date in ISO 8601;
the **PR** column is whichever PR landed the bump; the **JS** /
**CSS** columns are the **total** budgets after the change (in
bytes — same units the script asserts against). One-line rationale,
no soft-pedalling. Numbers that go down get a row too.

| Date | PR | Init JS | Init CSS | Total JS | Total CSS | Rationale |
|---|---|---|---|---|---|---|
| 2026-06-14 | data-table-multi-sort | 162000 | 64000 | 512000 | 274000 | feat(matches): multi-column ("Excel-style") sort for the Data-density table. `useTableSort` gains an ordered `SortLevel[]` stack (persisted via `usePersistedRef`) + a multi-key `compareMulti` fold + the `cycleSort`/`addLevel`/`removeLevel`/`moveLevel`/`setLevelDir`/`clearSort` mutators; `MatchesTable` grows Shift+click headers + numbered level badges. Net ~+283 B total JS (506283 over the prior 506000 cap), all in the lazy MatchesView chunk; initial JS flat at 101370. Bumped total JS to 512000 with headroom for the follow-up Custom Sort dialog PR. |
| 2026-06-13 | sfc-dense-views | 162000 | 64000 | 506000 | 274000 | refactor(sfc): Q2 — decomposing the dense view SFCs under 600 (MatchHeroModeBand → drill-nav + heatmap + matches SFCs; MatchesMembersList → MatchesTable; the remaining MatchesView / NarrowPopover / UnknownMapsView to follow). Each child SFC is a new module + its event-forwarding wiring; ~+1 KB total JS per view (502789 over the 502000 cap after MatchesTable), all in the lazy MatchesView chunk; initial JS flat at 101325. Bumped total JS to 506000 with headroom for the three remaining view decompositions. |
| 2026-06-13 | sfc-medium-trims | 162000 | 64000 | 502000 | 274000 | refactor(sfc): Q2 — trim the medium SFCs under 600 (MatchDetailPanel→useSmoothScroll, ProfileSwitcher→useProfileSwitcher, SettingsAdvanced→SupportedSourcesRow). Each extracted composable/child is a new module + (for the masthead-eager ProfileSwitcher) a small initial-chunk bump to 101325 (still far under the 162000 init cap); total JS +1 KB to 500104 over the 500000 cap. Bumped total JS to 502000. |
| 2026-06-13 | sfc-card-sections | 162000 | 64000 | 500000 | 274000 | refactor(sfc): Q2 MatchCardExpanded decomposition (part 2 of the markup/CSS-heavy split). Extracting the journal / status-choosers / heroes / rank / sources sections into child SFCs moves their scoped CSS out (total CSS flat — it just relocates between scoped blocks) but each child is a new module with props/emits + the parent's event-forwarding handlers, which Vite can't fold away: ~+1 KB total JS per pair of extractions (498495 over the prior 498000 cap), all in the lazy detail-panel chunk; initial JS flat at 100538. Bumped total JS to 500000 with headroom for the remaining card section (leaver) + the sibling oversized-SFC PRs. The card itself dropped 2858 → 612 lines across the part-2 PRs. |
| 2026-06-13 | sfc-splits | 162000 | 64000 | 498000 | 274000 | refactor(sfc): Q2 SRP first PR. Extract MatchCardExpanded's annotation editor into `useMatchAnnotationEditor` + the rule-of-three `useWindowMonths` (the follow-up the `hero-mode-row` row below explicitly flagged). A composable's public return-object keys can't be minified the way a component's inline `<script setup>` locals are, so the extraction costs ~178 B total JS (496178 over the prior 496000 cap), all in the lazy MatchesView / detail chunks; initial JS flat at 100538 (well under 162000). Bumped total JS to 498000 with a little headroom for the sibling markup/CSS-heavy SFC PR's sub-component extractions. |
| 2026-06-12 | hero-mode-drilldown | 162000 | 64000 | 496000 | 274000 | feat(matches): drill-down navigation stack for the Hero × Game-Mode band. Clicking a cell now narrows the page AND pushes a band-local stack (Hero×Mode → that mode's maps → that map's recent matches); a "Go back" button pops one level, reverting only the picks the band applied; a reconciliation watcher keeps the stack honest if filters are cleared from the rail. Adds two thin dossier aggregates (`mapCounts`, `recentMatches` + windowMonths) + the breadcrumb / maps-tile / recent-list render modes. Net ~+3.5 KB total JS (493561 over the prior 490000 cap), all in the lazy MatchesView chunk; CSS under cap. Bumped total JS to 496000. |
| 2026-06-12 | hero-mode-row | 162000 | 64000 | 490000 | 274000 | feat(matches): promote the Hero × Game-Mode heatmap from an opt-in grid widget to a full-width dossier ROW (sibling of Campaign Log / Geography). New `MatchHeroModeBand.vue` (migrated heatmap + a 1M/3M/6M/12M trailing-window picker feeding a new `windowMonths` param on the `heroGameModeCounts` dossier helper + an inline gear reusing `WidgetConfigPopover`); registered in `useSectionLayout`, removed from the widget registry. Net ~+1.8 KB total JS (488306 over the prior 488000 cap) — the new window-picker + gear + larger band template, all in the lazy MatchesView chunk. CSS up ~2.9 KB but well under cap; initial budgets unchanged. Bumped total JS to 490000. (Window-picker `loadWindow`/`pickWindow` now duplicated across Campaign Log + Geography + this band — a rule-of-three `useWindowMonths` extraction is the natural follow-up.) |
| 2026-06-12 | teammate-analysis | 162000 | 64000 | 488000 | 274000 | feat: teammate (member) analysis — a new narrow filter dimension (clone of tags, AND/stack semantics: pickedMembers + matchesMembers + chip-cloud picker) plus an opt-in win-rate-by-teammate dossier widget (`withWhomBreakdown` aggregate + WithWhomWidget). All in the lazy MatchesView chunk. Net ~+3.6 KB total JS (484869 over the prior 484000 cap). Bumped total JS to 488000; other budgets unchanged. Closes REVIEW.md D1 — the feature aggregates in memory, so no member DB index is needed. |
| 2026-06-11 | split-matchesview | 162000 | 64000 | 484000 | 274000 | refactor(matches): split MatchesView.vue into SFCs (REVIEW.md D2). Extracting MatchesArchiveDrawer globalized the contextual multi-select checkbox CSS (the combined `.leaf-checkbox, .archive-checkbox` selectors — shared by the live leaf row + the archive row) into app.css per the >1-component rule: +376 B initial CSS (63376 over the prior 63000 cap). The split otherwise SHRINKS the lazy MatchesView chunk substantially as its ~1.4 K-line scoped block redistributes to per-SFC chunks. Bumped initial CSS to 64000. |
| 2026-06-11 | geography-gear-filter | 162000 | 63000 | 484000 | 274000 | feat(matches): gear/display filter for the Geography (Map × Role) band. New `useMapRoleConfig` (persisted roles/mapTypes/maps sets, empty=all) + `MapRoleConfigPopover` (role + map-type pills, map search/checklist, Reset) + band wiring (client-side row/column filtering, empty state). All in the lazy MatchesView chunk. Net ~+5.4 KB total JS (483377 over the prior 478000 cap) + ~+5.6 KB total CSS (still under the 274000 cap). Bumped total JS to 484000; initial unchanged. |
| 2026-06-10 | modal-scroll-lock | 162000 | 63000 | 478000 | 274000 | fix(matches): freeze background scroll under every overlay (no jump). New `useScrollLock` (ref-counted overflow lock + non-passive wheel-block that cancels background scroll so WKWebView can't queue a pending scroll that snaps on close) baked into `useModalFocusTrap` + direct calls on the five non-trap overlays + the onboarding tour. Net ~+1.2 KB total JS (477225 over the prior 476000 cap). Bumped total JS to 478000. Total CSS unchanged; initial unaffected (the composable rides the shared trap that's already in the eager App.vue path). |
| 2026-06-10 | dossier-no-edit-mode | 162000 | 63000 | 476000 | 274000 | refactor(matches): remove dashboard edit mode. Deleted DashboardCustomizer/EditBanner/AddTile (modal + banner + tile) and added DossierManageMenu (compact Add dropdown) + DossierSection (inline section chrome) + useSectionLayout. Total CSS DROPPED ~8.7 KB (262477, the deleted modal/banner styles outweigh the new ones); total JS net ~+1 KB. Initial JS rose ~290 B over the 161000 cap (161290) — a Vite chunk-heuristic shift from the 3-deleted / 3-added component reshuffle (same behavior the 2026-06-08 technical-debt-sweep row documented), not a new eager import. Bumped initial to 162000 for headroom. |
| 2026-06-10 | map-role-band | 161000 | 63000 | 476000 | 274000 | feat(matches): Geography Map × Role performance band (full-width heatmap after the Campaign Log) + 1M window option on both heatmaps. New `MatchMapRoleBand.vue` (3 role rows × all-maps-grouped-by-type grid) + `mapRoleCounts` windowed dossier aggregate. Net ~3.7 KB total JS + ~3.4 KB total CSS — all in the lazy MatchesView chunk. Initial budgets UNCHANGED: `winrateVolumeFill` was deliberately split into `match-heatmap-helpers.ts` (band-only import) instead of `match-helpers.ts`, which App.vue pulls into the initial chunk — keeping the initial JS flat (160857 B, under the 161000 cap). |
| 2026-06-10 | design-system-tightening | 161000 | 63000 | 470000 | 268000 | refactor(ui): design system tightening (1.0 plan §C subset). Settings section dividers (1 selector + 4 lines) + Day theme pulse halo (box-shadow on records-pulse keyframes) + danger-confirm pulse keyframe + reduced-motion fallback in MatchCardDanger.vue. Net 30 B over the prior CSS cap (62030 vs 62000). Bumped to 63000 for headroom on PR #8 (split large components) which will shuffle some CSS between scoped blocks. |
| 2026-06-10 | ux-first-run-and-error-states | 161000 | 62000 | 470000 | 268000 | fix(ux): first-run + error states (1.0 plan §C). New `error-helpers.ts::plainLanguageError` translator (~7 raw-Go regex → CTA mappings) imported synchronously by App.vue + `setError`/`setErrorFromRaw`/`clearError`/`errorRetry` seams + Retry/Dismiss banner DOM. Net 264 B over the prior cap (160264 vs 160000). Bumped to 161000 for headroom on this PR's follow-ups in the same area; the translator is intentionally NOT lazy because it's called on every error-setting site at the top of App.vue. |
| 2026-06-09 | single-button-game-data | 160000 | 62000 | 470000 | 268000 | feat(updates)!: single "Update game data" button + diff preview manifest. UpdateCheckModal rewritten — Release sub-row deleted (smaller), but new freshness header + counts headline (display-font sizing) + manifest grid (3-col + kind chips + ± color states) add ~5 KB of CSS net. Total JS actually drops slightly because the release-channel state machine + ApplyDataUpdate wrapper are gone. |
| 2026-06-09 | live-data-channel | 160000 | 62000 | 470000 | 263000 | feat(updates): from-main YAML channel + Sync from main UI. UpdateCheckModal grew a second sub-row + state machine + ApplyMainDataUpdate wrapper + extended UpdateInfo with MainStatus. Net ~3 KB total JS over the previous bump (modal still lazy; the extra bytes are in the lazy chunk, not initial). |
| 2026-06-09 | updates-modal-banner | 160000 | 62000 | 465000 | 263000 | feat(updates)!: replace silent roster fetch with explicit modal + reminder. UpdateCheckModal (lazy ~6.6 KB JS + ~3 KB CSS) + UpdateReminderBanner (~2 KB JS + ~2 KB CSS) + useUpdateReminder + extended UpdateInfo wiring. Net ~6.5 KB total JS / ~8 KB total CSS / ~1.5 KB initial CSS (banner styles in App.vue). |
| 2026-06-08 | leaf-virtual-integration | 160000 | 60000 | 458000 | 254000 | Item 1 integration — useVirtualWindow wired into MatchesView for the flat (groupBy='none') leaf-row path. Net ~3 KB JS (window-mode geometry + spacer template branch + auto-scroll-into-view + measureLeafHeight). |
| 2026-06-08 | source-picker-callout | 160000 | 60000 | 455000 | 254000 | Item 13 surface B — ContextualCallout + useContextualCallout primitives landed in PR #235 but didn't ship in any chunk until a consumer (ScreenshotSourcePicker) wired them in. Net ~2 KB JS (callout SFC + composable) + ~1.2 KB CSS (callout styles). |
| 2026-06-08 | hero-maptype-heatmap | 160000 | 60000 | 450000 | 252000 | Item 2 — Hero × map-type heatmap dossier widget + the new provideNarrow/useNarrow injection seam wiring. Net ~3 KB JS (widget SFC + helper + narrow seam) + ~0.5 KB CSS (grid styles). |
| 2026-06-08 | ui-polish-set | 160000 | 60000 | 445000 | 250000 | UI polish set (items 3 / 4 / 5 / 6 / 7 / 8 / 10 / 12 from the UI recommendations backlog): TypeaheadDropdown + inline tag autocomplete + bulk-tag + extended right-click menu + leaf hover preview + smart-empty suggestions + saved-set presets + supported-formats surface + re-parse progress. Combined ~22 KB JS + ~8 KB CSS — CSS budget bumped during commit 7 (saved-set presets footer surface). |
| 2026-06-08 | technical-debt-sweep | 156000 | 60000 | 422000 | 242000 | match-helpers.ts split into 5 topic files. Total JS dropped slightly, but Vite's chunking heuristic hoisted the smaller new modules into the initial chunk, lifting initial JS ~10 KB. Tradeoff is intentional — readability of the topic split outweighs the chunk-boundary shift; total stays under cap. |
| 2026-06-08 | #227 | 146000 | 60000 | 422000 | 242000 | Parser tightening (length-gated fuzzy Pass-2) + the Reference-data-gaps UI on the Unknown tab landed alongside the screenshot-source picker. Combined coverage. |
| 2026-06-08 | #226 | 146000 | 60000 | 422000 | 242000 | Windows screenshot source picker (4-card grid) — net JS +14 KB across `ScreenshotSourcePicker.vue` + first-run wiring. CSS bumped to absorb the picker grid styles. |
| 2026-06-05 | #224 | 146000 | 60000 | 408000 | 230000 | Parser fuzzy fix + `hero_raw`/`map_raw` schema + Unknown-hero leaf chip + detail-panel gap banner. |
| pre-2026-06 | — | varies | varies | varies | varies | Prior bumps stacked one-line rationales onto the comment in `scripts/ci/check-bundle-size.sh`. Anyone curious can `git log -p scripts/ci/check-bundle-size.sh` to walk the older bumps; representative ones include `19febaf chore(bundle): bump JS+CSS budgets for the PR 6 sort+group dropdown` and the various PR D/E/F dashboard waves. |

## When to bump

- **A real feature needs the room.** The 2026-06-08 audit reset the
  floor lower than the previous "just bump it" drift; future bumps
  should be specific and justified.
- **Substantial modal surfaces should ride
  `defineAsyncComponent(() => import(...))`** so they land in their
  own chunk and stay out of the initial budget. The detail panel,
  screenshot lightbox, and cheatsheet are the pattern to mirror.
  `App.lazy-views.test.ts` is the regression guard that fails if a
  static import sneaks back in.
- **Don't bump to silence noise.** If a non-feature PR is pushing
  the budget, that's a bug — investigate before lifting the cap.

## Where this is checked

- `scripts/ci/check-bundle-size.sh` — the assertion. Run by CI's
  "Enforce bundle-size budget" step + lefthook's pre-push.
- `.github/workflows/ci.yml` — the CI invocation.
- `lefthook.yml` — the pre-push invocation (uses the script's
  defaults, no env overrides).
