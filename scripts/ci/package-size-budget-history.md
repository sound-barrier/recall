# Package & directory size budget history

The per-directory file budgets in `scripts/ci/package-size-budgets.txt` get
bumped when a grouping legitimately grows, and ratcheted down when a split
lands. This is the running record of why each number moved, so a future
maintainer doesn't have to `git blame` a comment block.

Append a row when you change a number. Date in ISO 8601; **PR** is whichever PR
landed the change; **From → To** is the budget column (mention the waiver in the
rationale if you touched it). One line, no soft-pedaling.

Numbers that go **down** get a row too — a ratchet after a split is the most
valuable row in this table, because it is the only evidence the debt was
actually paid rather than re-waived.

The two legitimate reasons for an upward bump: the new file is the same
responsibility spelled one concern wider, or the directory's responsibility
genuinely grew and splitting would separate things that change together. "The
gate was in the way" is not one of them.

| Date | PR | Directory | From → To | Rationale |
|---|---|---|---|---|
| 2026-08-16 | package-size-discipline | *(all)* | — → initial | Gate introduced. Budgets set to each directory's count on this date, zero headroom, except the six groupings with a split owed in this campaign (`frontend/tests/e2e`, `composables/matches`, `src/match`, `composables/shared`, `components/matches/list`, `components/shared`), which carry the target in the budget column and today's count as a temporary waiver so the file records intent instead of ratifying the sprawl. Frontend target = 20, the low end of the "~20–25" ceiling both CLAUDE.md files documented and nothing ever asserted. `pkg/app` registered at its current 54 **provisionally** — the split that pays it is designed but unmeasured, and guessing a target would be worse than recording the real one; the closing commit of this campaign replaces it. `frontend/src/client` exempt (generated). `dashboard/widgets` registered at 40 with no waiver: it is the `pkg/db` case on the frontend and the split is deliberately deferred. |
| 2026-08-16 | package-size-discipline | `frontend/src/composables/matches` | 20 (waived 51) → 20 | Waiver PAID. The 51 modules moved into subfolders mirroring `components/matches/`'s existing folder names — narrow (12), list (13), table (6), dossier (7), detail (4), manual (2), pivot (1), timeline (1), trends (2) — leaving `useMatchActions` + `useExportBundle` at the root because they serve the whole tab rather than one band of it. `useCardFocus` went to `composables/shared/`: its only importer is `stores/ui.ts` and it serves MatchesView and UnknownMapsView alike. Pure move, no renames. |
| 2026-08-16 | package-size-discipline | `frontend/src/composables/matches/list` | — → 13 | New registration, one over the default 12. The count is inherited from the split above, not grown: this is the set that backs `components/matches/list`, and reaching 12 by evicting one module would have broken the mirror the rule asks for. |
| 2026-08-16 | package-size-discipline | `frontend/src/match` | 20 (waived 45) → 20 | Waiver PAID. The 45 pure-helper modules split into the sub-domain folders their filename prefixes already named — `elo/`, `coach/`, `pivot/`, `dossier/`, `compare/`, `trends/`, `table/`, `export/` — leaving 8 at the root: the cross-domain ids with the biggest import fan-outs (`match-time-helpers` reaches 14 directories, `match-helpers` 8), which should never move again. Sub-domain folders rather than a top-level `src/elo/`, because this tree's top level is a layer taxonomy and one documented home for pure helpers is the rule people actually apply. Budget held at 20 rather than ratcheted to 8: it is the declared frontend ceiling, and a helper landing back at this root should be a deliberate choice, not the default. |
| 2026-08-16 | package-size-discipline | `frontend/src/match/elo` | — → 13 | New registration, one over the default 12. The count is inherited from the split above, not grown: these thirteen modules were one coherent `elo-*` family before the regroup, and splitting them further would separate the decay-aware simulator from the closed forms that must agree with it — the invariant the Elo coherence campaign exists to hold. The four `components/elo/*.ts` view helpers deliberately did NOT move here; `elo-chart-options.ts` imports a type from `components/matches/trends/`, so relocating it would invert the layering. |
| 2026-08-16 | package-size-discipline | `frontend/src/components/matches/list` | 20 (waived 22) → *(deleted)* | Waiver PAID. The list folder was four concerns: the data table (`MatchesTable` + `MatchTableRow` + their two stylesheets + the sort popover → `matches/table/`), four toasts that are overlay chrome rendered only by `AppOverlays` and never by a list row (→ `matches/toasts/`, the placement `dashboard/DashboardUndoToast.vue` already set), the bulk/archive bars (→ `matches/bulk/`), and the leaf-row list itself, which stays. That leaves 10 files at this root, UNDER the default 12, so the entry is deleted rather than re-declared at a number nothing needs — registration is earned by size, and a folder that no longer earns one should not keep it. Its mirror `composables/matches/list` stays registered because it is at 13. |
| 2026-08-16 | package-size-discipline | `frontend/src/components/coach` | 19 → *(deleted)* | Split into `room/` (10), `reel/` (3), `notes/` (3) and `inbox/` (3), leaving zero files at this root — the gate rejects an entry naming a directory with no counted files, so the line goes. The taxonomy is not invented: it is the store boundary Phase 4 creates. `inbox/` holds exactly the PLAYER-side set (`CoachInboxBanner`, `CoachReturnCard`, `CoachReturnSheet`) that will read the new `stores/coachReturns.ts`, so folder and store agree; `room/` is the coach-side film room and keeps `coach-room-props.ts`, the prop vocabulary all four folders share. Each subfolder answers to the default 12, which preserves what the deleted entry bought — the next coaching file is a deliberate choice — at a finer grain than one number over 32 files could. |
| 2026-08-16 | package-size-discipline | `frontend/src/components/settings` | 18 → 15 | Ratchet after eviction, not a split. `ExportBundleModal` went to `components/matches/export/` (it drives `composables/matches/useExportBundle` and is opened from the Matches bulk bar) and the `IgnoredFilesPanel`/`IgnoredFileRow` pair to `components/ingest/ignored/` (backed by `composables/ingest/useIgnoredScreenshots`). All three were only RENDERED from Settings' overlay stack, which is a mounting point, not ownership. Budget ratcheted to the new count rather than left at 18: three files of headroom on a folder that just shed three is exactly the silent growth this gate exists to catch. |
| 2026-08-16 | package-size-discipline | `frontend/tests/e2e` | 20 (waived 184) → 11 | Waiver PAID, and the target ratcheted below itself. The 173 specs moved into sixteen feature folders (`matches` 25, `dossier` 23, `settings` 19, `match` 17, `data-table` 16, `app` 13, `narrow` 12, `unknown` 9, `onboarding` 6, `parse` 6, `a11y` 5, `dashboard` 5, `trends` 5, `update` 5, `coach` 4, `elo` 3), leaving only the shared harness at this root: eight `_*.ts` helpers plus the three `coverage-*.ts` files playwright.config.ts names by path. 11, not the 20 this entry estimated — the estimate said "around 14" and guessed high, and a budget with nine files of slack is not a budget. The two `*-snapshots/` directories stayed put and `snapshotPathTemplate` is untouched: `{testDir}` is the CONFIGURED test dir and `{testFileName}` is the basename, so a nested spec still resolves to the same baseline directory. Proof the move was lossless: `npx playwright test --list` reports 809 tests in 173 files, identical before and after, and both `*-webkit.spec.ts` specs still bind to the webkit project (its `testMatch` regex is matched against the full path and is `.*`-prefixed). Pure move; the only content change is 224 helper imports going from `'./_x'` to `'../_x'`. |
| 2026-08-16 | package-size-discipline | `frontend/tests/e2e/{app,data-table,dossier,match,matches,narrow,settings}` | — → 13 / 16 / 23 / 17 / 25 / 12 / 19 | Seven new registrations, all inherited from the split above rather than grown. The other nine folders sit under the default 12 and stay unregistered. `narrow` is registered AT the default so the limit is a number someone chose rather than a coincidence. `matches` is the largest at 25 because it is the app's primary view and its specific bands (narrow, trends, data-table, dossier) are already carved out of it; `dossier` folds in the two-spec Compare tab rather than carving the two-file folder the gate's own failure text warns against. A spec count is a FEATURE count here — the TDD rule mandates one per user-visible affordance — so these numbers are expected to move upward with real features, one considered bump at a time. |
| 2026-08-16 | package-size-discipline | `pkg/app` | 54 (provisional) → 47 | **The row this campaign exists to produce.** The provisional entry recorded 54 because the split was designed but unmeasured; this replaces it with the count after the carve. 54 → 47 files, 7,607 → 5,673 LOC (−25%), six leaf packages out: `pkg/sse`, `pkg/seed`, `pkg/matchedit`, `pkg/snapshot`, `pkg/screenshot`, `pkg/release`. The file count fell less than the LOC did, and that is the honest shape of the result — each carve leaves an `*_alias.go` behind, so the shell trades ~400-line implementation files for ~40-line re-export files. Nine of those shims now hold 358 LOC, and they are load-bearing rather than residue: `pkg/cmd` maps 41 `app.Err*` sentinels to problem+json statuses inline across seven files, and re-declaring one instead of aliasing it is a silent 500 with no compile error. `pkg/cmd/problems_ladder_test.go` pins all 41, plus a leaf-identity assertion that a same-message re-declaration would fail. Not carved, deliberately, and recorded in TECHNICAL_DEBT.md §3: the five mutex-guarded clusters (settings, tesseract, watcher, parse, coach session), the mutually recursive parse pipeline (carving it needs a ~10-method host interface — the interface-pollution smell), the `app_wails`/`app_server` twins, and profile lifecycle. Tesseract stays for a security reason, not a cohesion one: `safePathChars` is the CodeQL-recognized sanitizer for two sinks in two files, and carving tesseract alone separates it from one of them. |
| 2026-08-16 | package-size-discipline | `frontend/src/{match,composables/matches,composables/shared,components/shared}` | 20 → *(deleted)* | **Corrects four rows above in this same campaign.** Each of these was emptied by a Phase 3 split and kept its pre-split budget of 20, leaving `src/match` 8/20, `composables/matches` 2/20, `composables/shared` 9/20 and `components/shared` 2/20 — entries that granted twelve, eighteen, eleven and eighteen files of ungated growth on folders the campaign had just cleaned out. Deleting them drops each to the default 12, which is STRICTER than the line it replaces. This is the precedent `components/matches/list` and `components/coach` already set two rows up ("registration is earned by size"); it was applied by hand there and missed here, which is the exact drift this file exists to catch. The `src/match` row's stated reason — budget held at 20 so "a helper landing back at this root should be a deliberate choice, not the default" — had it backwards: 20 meant the next twelve helpers landed with no decision at all, where the default makes the ninth trip the gate. |
| 2026-08-16 | package-size-discipline | *(gate)* | — | Not a budget change: `check-package-size.sh` gained the check that FINDS the row above. It already errored when a waiver was paid (debt cleared, entry must go); it now errors symmetrically when a budget goes slack — an entry whose budget exceeds the default while the directory sits under the default asserts nothing, since deleting it would tighten the limit. Registering BELOW the default to be deliberately stricter stays legal; only a budget above the default is checked, so `tests/e2e/narrow` at 12/12 is untouched. Verified by running it against the four offenders and the two controls before the entries were deleted. |
| 2026-08-16 | package-size-discipline | *(gate)* | — | Not a budget change: adversarial review of the gate itself, nine findings, all fixed here. **A genuine fail-open** — a directory whose name contained a SPACE measured zero files and PASSED, because the counter emitted space-separated pairs that pass 2 re-split on the default FS (30 files in `components/my feature/` → exit 0, and `--list` printed a phantom `components/my  0 / 12`). Now tab-delimited, plus `core.quotePath=false` so a non-ASCII directory can be registered at all rather than only counted. **The slack check added one commit earlier was off by one and too narrow**: `n < default` missed a directory sitting exactly AT the default (a 12-file folder registered at 40 passed) and said nothing about headroom above it (`pkg/db` could go 30 → 45 silently). Replaced by `n < max` — an entry must EQUAL its count — which is simpler, strictly stronger, and subsumes both; the unmutated file still exits 0 because all 21 numeric entries were already exact. Also: `*.test.ts` exclusion scoped to `frontend/src` (Playwright's default testMatch accepts `*.test.ts`, so an e2e spec so named ran in CI while being invisible to its folder budget — zero such files today, so the fix is a no-op on the tree and closes the hole), `-snapshots/` anchored to `frontend/tests` (it was unanchored, so any directory could opt out of counting by name), and `DEFAULT_MAX_FILES` validated (a non-numeric value made awk compare strings, passing all 93 unregistered directories while printing `3 / 0` — it loosened everything and said nothing). Three prose claims were wrong and are corrected: `pkg/cmd` said "ten of the nineteen are !serveronly" (it is four), `frontend/src` said 23 files / nine test siblings (24 / 10), and the row above this one had the four deleted entries' counts with the first and last swapped. A pre-push hook now asserts that a diff touching the budget file also touches THIS file — branch-wide, with its limit written down. |
| 2026-08-17 | rank-percentile | `pkg/parser` · `components/dashboard/widgets` · `tests/e2e/match` | 22 → 23 · 40 → 41 · 17 → 18 | Three upward bumps, each the same responsibility spelled one concern wider. `pkg/parser` gains `modifiers.go`, the accessor over the new `modifiers.yaml` — the exact shape `ranks.go` already has beside `ranks.yaml`, and one loader per embedded YAML is the pattern this package spells file-per-file. `components/dashboard/widgets` gains one widget (`RankPercentileWidget`), which is what that directory is FOR; it remains the frontend's `pkg/db` case, deliberately unsplit. `tests/e2e/match` gains one spec, and a spec count is a feature count here — the TDD rule mandates one per user-visible affordance, so this number is expected to climb with real features, one considered bump at a time. |
| 2026-08-17 | usability-campaign | `frontend/tests/e2e/match` | 18 → 21 | Two specs for the Rank Update block, both mandated by the TDD rule. **The first bump is late and that is the finding**: `rank-change-sign.spec.ts` landed earlier on this same branch and took the directory to 19/18 without a bump, so the gate was RED for several commits and nobody noticed — because the budget check is not part of `task lint` or `task test`, only of CI's `lint` job and the pre-push hook, and neither had run yet. The lesson is the one already written in the campaign plan and not followed: run `task package-size-audit` after ADDING a file, not before pushing. The second is `rank-unknown-modifier.spec.ts`, which proves an unrecognized modifier chip survives parser → SQLite → wire → render instead of existing only in a log line. The third is `rank-partial.spec.ts`, which covers a rank screen the parser could only partly read — the block rendering at all, and the hand-fill for a tier the override layer already accepted but no UI reached. All three are the same responsibility spelled one concern wider — the detail panel's rank block gaining contracts — not a new grouping. |
| 2026-08-17 | usability-campaign | `frontend/src/components/dashboard/widgets` · `frontend/tests/e2e/dossier` | 41 → 44 · 23 → 24 | Phase 2's three climb-insight widgets and their e2e. The three land together because they share one kernel (`match-baseline-helpers`: disjoint trailing windows, a two-proportion z, and the climb verdict), not because the directory grew a new family — which is exactly the condition the entry above says would earn a split instead. The directory keeps its "correctly-large" standing: one responsibility (render one dossier slice as a tile), one intentional public surface (the registry). |
| 2026-08-17 | usability-campaign | `frontend/tests/e2e/narrow` | 12 → 13 | One spec for natural-language date phrases. It is the same responsibility spelled one concern wider — a third way to reach the time scope the chips and the From/To pickers already write, not a new grouping — and most of its cases are about what the parser REFUSES, which is the half of the feature worth pinning. |
| 2026-08-17 | usability-campaign | `frontend/tests/e2e/app` | 13 → 14 | One spec for the command palette. It belongs with the app-shell specs because that is what it is: a chord that reaches any view from anywhere, owned by no single feature. |
| 2026-08-17 | usability-campaign | `pkg/parser` | 23 → 25 | parse_rank.go (715 lines) split five ways by what it reads, less the three Tesseract-invocation files carved out to the new `pkg/tesseract`. |
| 2026-08-17 | usability-campaign | `pkg/fixtures` | default 12 → 13 | fixtures_rank.go (682 lines) split four ways: the ladder board, the skill/win-rate model and its constants, the per-track season walk, and the driver. |
| 2026-08-17 | usability-campaign | `frontend/src/composables/matches/list` | 13 → 15 | useMatchesRowActions (wraps the row-context state machine with its actions) and useMatchesBulkActions (the wiring between live selection, archive selection and the shared move picker), both carved out of MatchesView.vue. |
| 2026-08-18 | coaching-moments | `frontend/tests/e2e/match` | 21 → 22 | The player's own cue strip in the match journal — its own route, table and write gate, so its own spec. |

## 2026-08-18 — frontend/src/components/app 12 (default) → 13 (#700)

The coaching receipt strip (`NoticeBanner.vue`) is the 13th file in the
App-shell chrome, which had been answering to the default.

A bump rather than a split. The folder is one responsibility — what the shell
paints around whichever view is up — and the only split available is a
`banners/` subfolder holding three files, which buys a directory and no
clarity. Registered at exactly 13. The first attempt declared 14, leaving a file of
slack "so the next banner would not need a rewrite" — and the gate refused it,
which is the whole point: a budget sitting above its directory's count is how
a folder silently regrows what it just shed.
| 2026-08-18 | reviews-tab | `frontend/src/components/app` | 13 → 14 | `lazy-view.ts`: the loading-skeleton + failed-chunk-reload wrapper carved out of App.vue so the film room inside the Reviews tab shares the tabs' failure surface (a bare `defineAsyncComponent` there rendered an empty tabpanel on a failed chunk). It sits beside the two fallbacks it composes. |
| 2026-08-18 | reviews-tab | `pkg/db` | 30 → 31 | `store_self_review.go`: the player-authored self-review family (six STRICT tables) beside the coach-authored and coach-received ones — the same responsibility one concern wider. |
| 2026-08-18 | reviews-tab | `pkg/app` | 47 → 48 | `self_review.go`: the `*App` shell over the new `pkg/review` leaf — gate, store call, broadcast — like every other leaf shell. |
| 2026-08-18 | reviews-tab | `pkg/cmd` | 19 → 20 | `server_self_review.go`: the `/api/v1/self-reviews` endpoint group. |
| 2026-08-19 | reviews-tab | `frontend/tests/e2e` | 11 → 12 | `_reviews.ts`: the in-memory self-review server (sittings, notes, moments, the matches list carrying self blocks) behind the self-review specs — the ninth root helper the entry said would be a real decision. |
| 2026-08-19 | reviews-ux | `pkg/db` | 31 → 32 | `store_share_exports.go`: the sent ledger (share_exports + share_export_matches) — the receipt that a share left, a new sidecar concern beside the three coaching families. |
| 2026-08-19 | reviews-ux | `frontend/src/components/app` | 14 → 15 | `WhatsNewStrip.vue`: the one-time feature pointer (app chrome beside the banners); its gate composable rides `composables/app`, under budget. |
| 2026-08-19 | reviews-ux | `frontend/tests/e2e/app` | 14 → 15 | `whats-new.spec.ts`: the strip is shell chrome on every view, so its spec sits with the shell's. |
| 2026-08-20 | bulk-bar-float | `frontend/tests/e2e/matches` | 25 → 26 | `bulk-bar-layout.spec.ts`: the floating bar's geometry contract — rows must not move when it appears; every button inside at 1024/1440/1920. |
| 2026-08-20 | review-surfaces | `pkg/db` | 32 → 33 | `store_focus_items.go`: the three tables behind "what to work on" (coach-authored, the player's own sitting, and what a coach sent), replacing the single free-text `coach_session_summaries`. |
| 2026-08-20 | review-surfaces | `pkg/app` | 48 → 49 | `focus.go`: the player's assembled list — the one read spanning two coaching families, and the ordering (coach first, newest-first) two UI surfaces must agree on. |
| 2026-08-20 | review-surfaces | `pkg/cmd` | 20 → 21 | `server_focus.go`: the `/api/v1/focus` endpoint group — read the whole list, move one item along it. |
| 2026-08-20 | review-surfaces | `frontend/src/components/dashboard/widgets` | 44 → 45 | `FocusNowWidget.vue`: one more tile; the first here that reads server state rather than the dossier, because the focus list is about the player, not the narrowed set. |
| 2026-08-20 | review-surfaces | `frontend/tests/e2e` | 12 → 13 | `_session-sse.ts`: the faked parse event stream + local-date stamps, extracted once a second spec (the focus nudge) needed what the session tally already had. |
| 2026-08-20 | review-surfaces | `frontend/tests/e2e/matches` | 26 → 27 | `focus-nudge.spec.ts`: the third parse-driven toast, beside the session tally and the tilt nudge. |
| 2026-08-21 | wysiwyg-notes | `frontend/src/styles` | 18 → 19 | `note-prose.css`. The note's block rhythm became the shared contract between the read surface and the WYSIWYG editor. Scoped was not an option: Vue rewrites `:deep()` per component hash, so the editor would have needed its own copy of every rule — and two copies of "how a note looks" drift apart one rule at a time, which is exactly what a shared sheet prevents by construction. |
| 2026-08-21 | coach-replay-code | `frontend/tests/e2e/match` | 22 → 23 | `replay-code-canonical.spec.ts`: the journal's replay cell now caps, uppercases and PUTs the canonical code. One spec per user-visible affordance, and the keystroke → PUT body chain is the part unit tests cannot see. |
| 2026-08-21 | coach-replay-code | `pkg/app` | 49 → 50 | `coach_replay.go`: the second door onto the one coaching session — opened from typed replay codes instead of a loaned bundle. The critical section is shared (`claimCoachSessionWith`), so this file is the two things that genuinely differ: growing the reel mid-session, and recording what the coach observed. `coach_session.go` is 454 lines; a third concern would push it past the 500-line ceiling. |
| 2026-08-21 | coach-replay-code | `frontend/src/styles` | 19 → 20 | `coach-sheet.css`: page geometry for the standalone review a coach hands over. The app has no printed-page equivalent; everything else the sheet needs (tokens, themes, paper, note-prose) is inlined from the files already here, which is why this one is short. Replaces `pkg/coach/ledger.tmpl.html`, whose hand-copied CSS had drifted from `note-prose.css`. |
| 2026-08-22 | standards-debt | `frontend/src/components/settings` | 15 → 16 | `ProbeChip.vue`: the detect-result chip three surfaces had each pasted a copy of — first-run setup, the folders section, the Tesseract section — along with the same three-line dismissed-resets-on-a-new-message state machine. Their a11y labels had already drifted, so the label is a required prop: a fourth surface has to say what its chip is about rather than inherit whatever it was pasted from. |
| 2026-08-22 | standards-debt | `frontend/src` | 14 → 13 | api.ts became `frontend/src/api/`. The entry's own comment had named that as the move to make "if this grows"; it reached 865 lines and 97 wrappers. Ratcheted down because a budget above its count is headroom the gate does not allow in either direction. |
| 2026-08-22 | standards-debt | `frontend/src/api` | new → 13 | The wire surface split along the section comments that already divided it (TECHNICAL_DEBT.md section 18): 11 endpoint families + barrel + the app-facing type aliases. Largest module 172 lines, against 865 before. Consumers unaffected — api-client.ts imports the namespace, so the barrel keeps `@/api` meaning what it meant. |
| 2026-08-22 | standards-debt | `frontend/src/components/coach/room` | default → 12 | Registered at its count during the section 20 adjudication: the film room, checked for cohesion and left whole. A directory on the default has no stated reason for the next person to read. |
| 2026-08-22 | standards-debt | `frontend/src/composables/app` | default → 12 | Registered at its count during the section 20 adjudication: app-shell composables, one concern each. A directory on the default has no stated reason for the next person to read. |
| 2026-08-22 | standards-debt | `frontend/src/composables/matches/narrow` | default → 12 | Registered at its count during the section 20 adjudication: the narrow filter language, which must not be split by file type. A directory on the default has no stated reason for the next person to read. |
| 2026-08-22 | standards-debt | `frontend/src/match` | default → 12 | Registered at its count during the section 20 adjudication: the family-less match helpers; a new family earns a folder. A directory on the default has no stated reason for the next person to read. |
| 2026-08-22 | standards-debt | `pkg/db/dbtest` | default → 12 | Registered at its count during the section 20 adjudication: the Fake, whose count tracks pkg/db's families by construction. A directory on the default has no stated reason for the next person to read. |
| 2026-08-22 | standards-debt | `pkg/matchedit` | default → 12 | Registered at its count during the section 20 adjudication: one file per editable field. A directory on the default has no stated reason for the next person to read. |
| 2026-08-22 | standards-debt | `frontend/src/components/coach/notes` | 12 → default (9) | SheetFocusItems / SheetFocusTally / SheetRecord moved to `frontend/src/components/sheet`. They are a shared sheet-rendering family, not note-editor internals — `reviews/SelfReviewSheet.vue` had to reach into the coach's folder to render a self-review. |
| 2026-08-25 | coaching-s-cuts | frontend/src/components/coach/room | 12 → 13 | CoachOrphanNotes — the drawer for notes the session carries but cannot frame; the desk's responsibility spelled one concern wider. |
| 2026-08-31 | dismiss-and-parse-accounting | `frontend/src/styles` | 20 → 21 | toasts.css — one family for the bottom-right ladder, replacing seven private copies of the same chrome (jscpd 0.56% → 0.46%). The stylesheet layer's own responsibility, one concern wider. |
| 2026-08-31 | dismiss-and-parse-accounting | `frontend/src/components/matches/detail` | 15 → 16 | MatchExclusionChooser — the second annotation chooser on the journal (why a match doesn't count, beside who left / who threw). One concern wider, sharing the chip family the two now hold in common.
| 2026-08-31 | dismiss-and-parse-accounting | `frontend/tests/e2e/match` | 23 → 24 | match-exclusion-reason.spec.ts — one spec per user-visible affordance, and this one is the only place the schema → wire → narrow → both tally sites chain is proven whole.
| 2026-08-31 | dismiss-and-parse-accounting | `frontend/tests/e2e/match` | 24 → 25 | match-duplicate-link.spec.ts — the stored keep-separate verdict and the chip that navigates to the twin; one spec per user-visible affordance.
| 2026-08-31 | dismiss-and-parse-accounting | `pkg/db` | 33 → 34 | store_duplicate_link.go — the "keep separate" verdict, one sidecar concern in one file, like every other sidecar in this package.
| 2026-08-31 | dismiss-and-parse-accounting | `frontend/tests/e2e/match` | 25 → 26 | replay-code-shelf-life.spec.ts — a stored code from a past season says so; one spec per user-visible affordance.
| 2026-08-31 | dismiss-and-parse-accounting | `frontend/src/components/coach/room` | 13 → 14 | CoachStatRail — the player's tendencies on the frame's own hero and map, read out of the loaned corpus; one more surface of the one room.
| 2026-08-31 | dismiss-and-parse-accounting | `pkg/db` | 34 → 35 | store_coach_sessions.go — the coach's own sittings, coach-AUTHORED like store_coach_notes.go beside it.
| 2026-08-31 | dismiss-and-parse-accounting | `pkg/db/dbtest` | 12 → 13 | fake_coach_sessions.go — the Fake's half of the same family; this count tracks pkg/db's by construction.
| 2026-08-31 | dismiss-and-parse-accounting | `frontend/src/styles` | 21 → 20 | toasts.css moved to `components/shared/`. Every toast is lazily loaded, so routing the family through app.css put its chrome in the initial stylesheet; a sibling the toast components import is the same one definition in the chunks that use it.
| 2026-08-31 | dismiss-and-parse-accounting | `frontend/src/components/matches/detail` | 16 → 17 | match-choosers.css — the shared chooser chip family, moved out of styles/buttons.css for the same reason as the toasts.
| 2026-08-31 | seasonal-recap-and-attachments | pkg/db | 35 → 36 | `store_moment_images.go` — the content-addressed image store. One table, one file, like every sidecar family beside it. |
| 2026-08-31 | seasonal-recap-and-attachments | pkg/cmd | 21 → 22 | `server_moment_images.go` — the `/api/v1/moment-images` endpoint group. The fourth binary-body surface, and the only one taking a picture rather than an archive. |
| 2026-08-31 | seasonal-recap-and-attachments | pkg/app | 50 → 51 | `moment_images.go` — custody plus the serving handler. A sibling of `screenshot_files.go`: that one resolves a path the user chose, this one looks up bytes we stored by digest. |
| 2026-08-31 | seasonal-recap-and-attachments | frontend/tests/e2e/match | 26 → 27 | `match-moment-attachment.spec.ts` — the first file-drop in the suite, and the only proof of the upload → digest → save → serve chain end to end. |
| 2026-08-31 | seasonal-recap-and-attachments | frontend/src/components/dashboard/widgets | 45 → 49 | The analytics wave: `sr-climb-rate`, `sr-by-role`, `hero-concentration`, `queue-gap`. One widget per question, and the two SR tiles ship beside the meter-denominated ones rather than replacing them. |
| 2026-08-31 | seasonal-recap-and-attachments | frontend/src/components/dashboard/widgets | 49 → 50 | `patch-split` — the before/after comparison, which needed `patches.yaml` to exist at all. |
| 2026-08-31 | seasonal-recap-and-attachments | pkg/parser | 25 → 26 | `patches.go` — game-change instants, a sibling of `seasons.go` with the same embed and update path. |
| 2026-08-31 | seasonal-recap-and-attachments | frontend/src/components/dashboard/widgets | 50 → 51 | `hero-trend-lines` — the pool table names a hero's win rate; this one draws how it got there. |
| 2026-08-31 | seasonal-recap-and-attachments | pkg/db | 36 → 37 | `store_roster.go` — the saved roster. The first sidecar family `Clear()` deliberately leaves alone: a list of people is not a record of games played. |
| 2026-08-31 | seasonal-recap-and-attachments | pkg/db/dbtest | 13 → 14 | `fake_roster.go` — the Fake's half of the same family. |
| 2026-08-31 | seasonal-recap-and-attachments | pkg/cmd | 22 → 23 | `server_roster.go` — `/api/v1/roster`. A person, not a match. |
| 2026-08-31 | seasonal-recap-and-attachments | pkg/app | 51 → 52 | `roster.go` — trim, default the name to the tag, refuse a blank one, refuse during a loan. |
| 2026-08-31 | seasonal-recap-and-attachments | frontend/src/api | 13 → 14 | `roster.ts` — one file per endpoint family, like every sibling. |
| 2026-08-31 | seasonal-recap-and-attachments | frontend/src/components/settings | 16 → 18 | `SettingsRoster.vue` + its stylesheet. Settings owns all config, and a roster is config. |
| 2026-08-31 | seasonal-recap-and-attachments | frontend/tests/e2e/matches | 27 → 28 | `live-session-banner.spec.ts` — chrome, but what it reports is a session over the match corpus. |
| 2026-08-31 | seasonal-recap-and-attachments | frontend/tests/e2e/matches | 28 → 29 | `roster-lookup.spec.ts` — maintained in Settings, but what it proves is what a match's Group chip says. |
